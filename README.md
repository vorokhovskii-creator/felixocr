# Felix OCR Web

Веб-приложение для извлечения артикулов автозапчастей из изображений с использованием OpenRouter API и модели DeepSeek-R1.

## 📋 Описание

Felix OCR Web позволяет:
- Загружать скриншоты (PNG, JPG, WEBP)
- Извлекать текст (артикулы автозапчастей) через OCR
- Нормализовать результаты (исправление ошибок O→0, I→1, l→1)
- Копировать нормализованные значения в буфер обмена

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- pip

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository>
cd FelixOCRWeb
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env`:
```bash
cp .env.example .env
```

5. Заполните `.env` с вашим API ключом:
```
OPENROUTER_API_KEY=sk-your-key-here
OPENROUTER_MODEL=deepseek/deepseek-r1:free
```

### Запуск

```bash
flask run
```

Приложение будет доступно по адресу `http://localhost:5000`

## 📁 Структура проекта

```
FelixOCRWeb/
├── app.py                      # Flask приложение
├── openrouter_client.py        # Клиент для OpenRouter API
├── extractor.py                # Модуль обработки результатов
├── config.json                 # Правила нормализации
├── requirements.txt            # Зависимости Python
├── .env.example                # Пример переменных окружения
├── .gitignore                  # Git исключения
├── templates/
│   └── index.html              # Главная страница
├── static/
│   ├── style.css               # Стили
│   └── script.js               # Клиентский JavaScript
├── prompts/
│   ├── system_prompt.txt       # Системный промпт для модели
│   └── user_prompt.txt         # Пользовательский промпт
└── uploads/                    # Директория для временных файлов (автоматически создается)
```

## ⚙️ Конфигурация

### config.json

Содержит правила нормализации текста:

```json
{
  "replace_rules": {
    "O": "0",
    "I": "1",
    "l": "1"
  },
  "specific_replacements": {}
}
```

- `replace_rules`: Символы для замены (ключ → значение)
- `specific_replacements`: Регулярные выражения для замены

### Переменные окружения

- `OPENROUTER_API_KEY`: Ваш API ключ OpenRouter (обязательно)
- `OPENROUTER_MODEL`: Модель для использования (по умолчанию: `deepseek/deepseek-r1:free`)

## 🔧 API Endpoints

### POST /api/process

Обработает загруженное изображение.

**Параметры:**
- `file` (multipart/form-data): Изображение (PNG, JPG, WEBP)

**Ответ:**
```json
{
  "success": true,
  "data": {
    "raw_text": "Распознанный текст",
    "numbers": [
      {
        "raw": "О123М45Б",
        "normalized": "0123M45B"
      }
    ]
  }
}
```

### GET /api/health

Проверка статуса приложения.

**Ответ:**
```json
{
  "status": "ok"
}
```

## 🎯 Особенности

- ✅ Drag & Drop загрузка файлов
- ✅ Предпросмотр изображения
- ✅ Нормализация результатов OCR
- ✅ Кнопка копирования в буфер обмена
- ✅ Красивый и адаптивный интерфейс
- ✅ Поддержка множественных форматов изображений
- ✅ Обработка ошибок
- ✅ Легко расширяемая архитектура

## 📝 Нормализация текста

Применяются следующие правила (в порядке):
1. Замена символов согласно `replace_rules`
2. Применение регулярных выражений из `specific_replacements`
3. Преобразование в верхний регистр
4. Сохранение пробелов, дефисов, подчеркиваний

## 🐛 Решение проблем

### Ошибка: "OPENROUTER_API_KEY not found"
Убедитесь, что файл `.env` создан и содержит корректный API ключ.

### Ошибка: "File type not allowed"
Поддерживаются только форматы: PNG, JPG, JPEG, WEBP

### Ошибка при обработке изображения
Проверьте:
- Наличие интернет соединения
- Корректность API ключа
- Размер изображения (максимум 16 MB)

## 📚 Документация

- [OpenRouter API Documentation](https://openrouter.ai/docs)
- [DeepSeek-R1 Model Card](https://openrouter.ai/models/deepseek/deepseek-r1)
- [Flask Documentation](https://flask.palletsprojects.com/)

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Felix OCR Web Application
